version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: hielofriends-postgres-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: hielofriends_db
      POSTGRES_USER: dev_user
      POSTGRES_PASSWORD: SecureDevPassword123
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: